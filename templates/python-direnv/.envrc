# Direnv configuration for Python development
# This file tells direnv to use the Nix flake in this directory

# Use the flake.nix in this directory
use flake

# Optional: Load .env file if it exists (for local secrets/config)
dotenv_if_exists
